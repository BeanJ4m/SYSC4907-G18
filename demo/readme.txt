
pip install -r requirements.txt
just run infr.py it does the inference and creates a new csv file which would be written to the inference_output folder
then run app2.py it would create a flask server on port

